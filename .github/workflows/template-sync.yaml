name: Template Sync

on:
  schedule:
    - cron: "17 6 * * *"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: template-sync
  cancel-in-progress: false

jobs:
  sync:
    uses: NWarila/terraform-template/.github/workflows/reusable-template-sync.yaml@f6874413375a81f56d46e090f8376f72dc0d9f56
    with:
      template_ref: f6874413375a81f56d46e090f8376f72dc0d9f56
