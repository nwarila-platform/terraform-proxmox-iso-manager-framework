# terraform-proxmox-iso-manager-framework
Terraform module that manages the lifecycle of installer ISOs on Proxmox VE storage. Imported by per-OS Packer template repos to declaratively pin, download, and SHA-verify the ISO their build consumes.
