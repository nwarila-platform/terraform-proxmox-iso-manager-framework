# Basic Integration Fixture

This case is configured in `tools/ci/config.toml`.

Framework integration assembles an ephemeral Terraform workspace from `terraform/`
and imports the human-facing quickstart input from
`examples/single-environment/terraform.tfvars.example`.
